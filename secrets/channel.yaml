apiVersion: apps.open-cluster-management.io/v1
kind: Channel
metadata:
  name: health-check-secrets-channel
  namespace: canary
spec:
  type: Namespace
  pathname: canary
  sourceNamespaces:
  - canary

